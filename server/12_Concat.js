/*
    Concat : es un metodo Inmutable que sirve para concatenar
    2 arrays
*/

const elements = [1,1,2,2];
const othersElements = [3,3,4,4];

const concatenados= elements.concat(othersElements);
console.log(concatenados);